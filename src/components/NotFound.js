import React from 'react';

const NotFound = () => (
    <div style={styles.NotFoundStyle}>
        <h1>Page Not Found</h1>
    </div>
);

const styles = {
    NotFoundStyle: {
        textAlign: 'center'
    }
}

export default NotFound;